<%= form_with(model: student_app, local: true) do |form| %>
  <% if student_app.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(student_app.errors.count, "error") %> prohibited this student_app from being saved:</h2>

      <ul>
      <% student_app.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :firstname %>
    <%= form.text_field :firstname, id: :student_app_firstname %>
  </div>

  <div class="field">
    <%= form.label :lastname %>
    <%= form.text_field :lastname, id: :student_app_lastname %>
  </div>

  <div class="field">
    <%= form.label :category %>
    <%= form.text_field :category, id: :student_app_category %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
